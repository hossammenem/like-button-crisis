{
  "name": "likescountcrisis",
  "version": "1.0.0",
  "description": "trying to miss around to see the best way to determine if the request user has liked the post or not",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hossammenem",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  }
}
